'use client';"use client";



import { useState, useEffect } from 'react';import { useState } from 'react';

import { podcastEpisodes, podcastCategories } from '@/config/podcast-data';import { podcastEpisodes, podcastCategories } from '@/config/podcast-data';

import { Youtube, Music, Radio } from 'lucide-react';

export default function PodcastsPage() {

export default function PodcastsPage() {  const [selectedCategory, setSelectedCategory] = useState<string>('all');

  const [selectedCategory, setSelectedCategory] = useState<string>('all');

  const [isLoading, setIsLoading] = useState(true);  const filteredEpisodes = selectedCategory === 'all' 

  const [error, setError] = useState<string | null>(null);    ? podcastEpisodes 

  const [episodes, setEpisodes] = useState(podcastEpisodes);    : podcastEpisodes.filter(episode => episode.category === selectedCategory);



  useEffect(() => {  return (

    try {    <main className="min-h-screen bg-gray-50 dark:bg-gray-900">

      setIsLoading(true);      <div className="container mx-auto px-4 py-12">

      setError(null);        <div className="max-w-3xl mx-auto text-center mb-16">

                <h1 className="text-4xl md:text-5xl font-bold font-hindi text-orange-600 mb-4">

      const timer = setTimeout(() => {            पॉडकास्ट

        const filtered = selectedCategory === 'all'           </h1>

          ? podcastEpisodes           <p className="text-lg font-hindi text-gray-600 dark:text-gray-400">

          : podcastEpisodes.filter(episode => episode.category === selectedCategory);            आध्यात्म और विज्ञान के अनूठे मेल को सुनें

                  </p>

        setEpisodes(filtered);        </div>

        setIsLoading(false);

      }, 500);        {/* Category Filter */}

        <div className="max-w-4xl mx-auto mb-12">

      return () => clearTimeout(timer);          <div className="flex flex-wrap justify-center gap-3">

    } catch (err) {            <button

      setError('एपिसोड लोड करने में समस्या हुई। कृपया पुनः प्रयास करें।');              onClick={() => setSelectedCategory('all')}

      setIsLoading(false);              className={`px-6 py-2 rounded-full text-sm font-medium transition-all ${

    }              selectedCategory === 'all'

  }, [selectedCategory]);                ? 'bg-orange-600 text-white'

                : 'bg-gray-200 dark:bg-gray-700 text-gray-800 dark:text-gray-200'

  if (error) {            }`}

    return (          >

      <div className="min-h-screen flex items-center justify-center">            सभी एपिसोड / All Episodes

        <div className="text-center p-8 bg-red-50 dark:bg-red-900 rounded-lg">          </button>

          <h2 className="text-2xl font-hindi text-red-600 dark:text-red-400 mb-4">त्रुटि</h2>          {Object.entries(podcastCategories).map(([key, value]) => (

          <p className="font-hindi text-gray-800 dark:text-gray-200">{error}</p>            <button

        </div>              key={key}

      </div>              onClick={() => setSelectedCategory(key)}

    );              className={`px-4 py-2 rounded-full ${

  }                selectedCategory === key

                  ? 'bg-orange-600 text-white'

  return (                  : 'bg-gray-200 dark:bg-gray-700 text-gray-800 dark:text-gray-200'

    <main className="min-h-screen bg-gray-50 dark:bg-gray-900">              }`}

      <div className="container mx-auto px-4 py-12">            >

        <div className="max-w-3xl mx-auto text-center mb-16">              {value.hi}

          <h1 className="text-4xl md:text-5xl font-bold font-hindi text-orange-600 mb-4">            </button>

            पॉडकास्ट          ))}

          </h1>        </div>

          <p className="text-lg font-hindi text-gray-600 dark:text-gray-400">        

            आध्यात्म और विज्ञान के अनूठे मेल को सुनें        <div className="grid md:grid-cols-2 lg:grid-cols-3 gap-6">

          </p>          {filteredEpisodes.map((episode) => (

        </div>            <div key={episode.id} className="bg-white dark:bg-gray-800 p-6 rounded-lg shadow-lg">

              <div className="flex items-center justify-between mb-4">

        <div className="max-w-4xl mx-auto mb-12">                <span className="text-sm font-medium text-orange-600 dark:text-orange-400">

          <div className="flex flex-wrap justify-center gap-3">                  Episode {episode.episode}

            <button                </span>

              onClick={() => setSelectedCategory('all')}                <span className="text-sm text-gray-500">{episode.duration}</span>

              className={'px-6 py-2 rounded-full text-sm font-medium transition-all ' +               </div>

                (selectedCategory === 'all'              <h3 className="text-xl font-hindi font-bold text-gray-900 dark:text-white mb-3">{episode.title.hi}</h3>

                  ? 'bg-orange-600 text-white shadow-md'              <p className="font-hindi text-gray-600 dark:text-gray-400 mb-6 line-clamp-3">

                  : 'bg-white dark:bg-gray-800 text-gray-700 dark:text-gray-300 hover:bg-orange-50 dark:hover:bg-gray-700'                {episode.description.hi}

              )}              </p>

              disabled={isLoading}              <div className="flex items-center justify-between">

            >                <div className="flex gap-3">

              सभी एपिसोड                {episode.youtubeUrl && (

            </button>                  <a 

            {Object.entries(podcastCategories).map(([key, value]) => (                    href={episode.youtubeUrl} 

              <button                    target="_blank" 

                key={key}                    rel="noopener noreferrer"

                onClick={() => setSelectedCategory(key)}                    className="inline-flex items-center gap-2 px-4 py-2 rounded-lg bg-red-600 text-white hover:bg-red-700 transition-colors"

                className={'px-6 py-2 rounded-full text-sm font-medium transition-all ' +                   >

                  (selectedCategory === key                    <Youtube size={18} />

                    ? 'bg-orange-600 text-white shadow-md'                    <span>YouTube</span>

                    : 'bg-white dark:bg-gray-800 text-gray-700 dark:text-gray-300 hover:bg-orange-50 dark:hover:bg-gray-700'                  </a>

                )}                )}

                disabled={isLoading}                <a 

              >                  href={episode.spotifyUrl} 

                {value.hi}                  target="_blank" 

              </button>                  rel="noopener noreferrer"

            ))}                  className="inline-flex items-center gap-2 px-4 py-2 rounded-lg bg-green-600 text-white hover:bg-green-700 transition-colors"

          </div>                >

        </div>                  <Music size={18} />

                  <span>Spotify</span>

        <div className="max-w-6xl mx-auto">                </a>

          {isLoading ? (              </div>

            <div className="grid md:grid-cols-2 gap-8">              <div className="mt-4 text-sm text-gray-500">

              {[1, 2].map((n) => (                {new Date(episode.date).toLocaleDateString('hi-IN')}

                <div key={n} className="bg-white dark:bg-gray-800 rounded-xl p-6 shadow-sm animate-pulse">              </div>

                  <div className="h-4 bg-gray-200 dark:bg-gray-700 rounded w-1/4 mb-4"></div>            </div>

                  <div className="h-8 bg-gray-200 dark:bg-gray-700 rounded w-3/4 mb-4"></div>          ))}

                  <div className="h-20 bg-gray-200 dark:bg-gray-700 rounded mb-4"></div>        </div>

                  <div className="flex gap-3">

                    <div className="h-10 bg-gray-200 dark:bg-gray-700 rounded w-24"></div>        {/* Social Links */}

                    <div className="h-10 bg-gray-200 dark:bg-gray-700 rounded w-24"></div>        <div className="max-w-2xl mx-auto mt-16 text-center">

                  </div>          <h2 className="text-xl font-hindi font-semibold text-gray-900 dark:text-white mb-6">अन्य प्लेटफॉर्म्स पर भी सुनें</h2>

                </div>          <div className="inline-flex flex-wrap justify-center gap-4">

              ))}            <a 

            </div>              href="https://music.amazon.in/podcasts/b7b42549-18e4-4ddb-814a-5b5e91e97644/%F0%9F%8E%A7-chhupa-hua-bharat-%E2%80%93-bhakti-hi-vigyan"

          ) : (              target="_blank"

            <div className="grid md:grid-cols-2 gap-8">              rel="noopener noreferrer"

              {episodes.map((episode) => (              className="inline-flex items-center gap-2 px-6 py-3 rounded-full bg-blue-600 text-white hover:bg-blue-700 transition-colors"

                <div key={episode.id} className="bg-white dark:bg-gray-800 rounded-xl p-6 shadow-sm hover:shadow-md transition-shadow">            >

                  <div className="flex items-center justify-between mb-4">              <Music size={20} />

                    <span className="text-sm font-medium text-orange-600 dark:text-orange-400">              <span>Amazon Music</span>

                      Episode {episode.episode}            </a>

                    </span>            <a 

                    <span className="text-sm text-gray-500">{episode.duration}</span>              href="https://www.jiosaavn.com/shows/-chhupa-hua-bharat-%e2%80%93-bhakti-hi-vigyan.-/1/9yLYvynnX68_"

                  </div>              target="_blank"

                  <h3 className="text-xl font-hindi font-bold text-gray-900 dark:text-white mb-3">              rel="noopener noreferrer"

                    {episode.title.hi}              className="inline-flex items-center gap-2 px-6 py-3 rounded-full bg-purple-600 text-white hover:bg-purple-700 transition-colors"

                  </h3>            >

                  <p className="font-hindi text-gray-600 dark:text-gray-400 mb-6 line-clamp-3">              <Radio size={20} />

                    {episode.description.hi}              <span>JioSaavn</span>

                  </p>            </a>

                  <div className="flex items-center justify-between">          </div>

                    <div className="flex gap-3">        </div>

                      {episode.youtubeUrl && (      </div>

                        <a     </main>

                          href={episode.youtubeUrl}   );

                          target="_blank"               <a href="https://youtube.com/@chhupahuabharat" target="_blank" className="bg-red-600 text-white px-4 py-2 rounded hover:bg-red-700">

                          rel="noopener noreferrer"                YouTube

                          className="inline-flex items-center gap-2 px-4 py-2 rounded-lg bg-red-600 text-white hover:bg-red-700 transition-colors"              </a>

                        >              <a href="https://open.spotify.com/show/06XoN5CPJllRhWW7YT4UU3" target="_blank" className="bg-green-600 text-white px-4 py-2 rounded hover:bg-green-700">

                          <Youtube size={18} />                Spotify

                          <span>YouTube</span>              </a>

                        </a>            </div>

                      )}          </div>

                      <a         </div>

                        href={episode.spotifyUrl}         

                        target="_blank"         <div className="mt-12 text-center">

                        rel="noopener noreferrer"          <a href="/" className="bg-orange-500 hover:bg-orange-600 text-white font-bold py-3 px-8 rounded-lg transition-colors">

                        className="inline-flex items-center gap-2 px-4 py-2 rounded-lg bg-green-600 text-white hover:bg-green-700 transition-colors"            ← वापस होम

                      >          </a>

                        <Music size={18} />        </div>

                        <span>Spotify</span>      </div>

                      </a>    </main>

                    </div>  );

                    <span className="text-sm text-gray-500">}

                      {new Date(episode.date).toLocaleDateString('hi-IN')}
                    </span>
                  </div>
                </div>
              ))}
            </div>
          )}
        </div>

        <div className="max-w-2xl mx-auto mt-16 text-center">
          <h2 className="text-xl font-hindi font-semibold text-gray-900 dark:text-white mb-6">
            अन्य प्लेटफॉर्म्स पर भी सुनें
          </h2>
          <div className="inline-flex flex-wrap justify-center gap-4">
            <a 
              href="https://music.amazon.in/podcasts/b7b42549-18e4-4ddb-814a-5b5e91e97644/%F0%9F%8E%A7-chhupa-hua-bharat-%E2%80%93-bhakti-hi-vigyan"
              target="_blank"
              rel="noopener noreferrer"
              className="inline-flex items-center gap-2 px-6 py-3 rounded-full bg-blue-600 text-white hover:bg-blue-700 transition-colors"
            >
              <Music size={20} />
              <span>Amazon Music</span>
            </a>
            <a 
              href="https://www.jiosaavn.com/shows/-chhupa-hua-bharat-%e2%80%93-bhakti-hi-vigyan.-/1/9yLYvynnX68_"
              target="_blank"
              rel="noopener noreferrer"
              className="inline-flex items-center gap-2 px-6 py-3 rounded-full bg-purple-600 text-white hover:bg-purple-700 transition-colors"
            >
              <Radio size={20} />
              <span>JioSaavn</span>
            </a>
          </div>
        </div>
      </div>
    </main>
  );
}